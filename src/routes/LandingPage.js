import React, { Component } from "react"
import {Layout, P, Input, Button} from '../styled/global.js'
 class Modal extends Component{
     render(){
         return(
             <Layout >
                 
             </Layout>
         )
     }
 }
 export {Modal}
